---
name: dever-{{COMPONENT_NAME}}
description: Use when 修改 {{COMPONENT_NAME}} Dever 组件，包括 model、service、provider、api、front/page JSON、front 插件、config、组件元数据、权限、公开接口和升级影响。
version: 0.1.0
---

# {{COMPONENT_TITLE}}

本组件 skill 需要和 `shemic-dever` 一起使用。先读 `shemic-dever` 的框架规则，再应用这里的组件专属规则。

## 事实来源

- 组件源码：`package/{{COMPONENT_NAME}}`
- 组件页面：`package/{{COMPONENT_NAME}}/front/page`
- 组件服务端模板：`package/{{COMPONENT_NAME}}/front/template`
- 组件模板静态资源：`package/{{COMPONENT_NAME}}/front/assets`
- 组件 front 插件：`package/{{COMPONENT_NAME}}/front/src/plugin.ts`
- 组件元数据：`package/{{COMPONENT_NAME}}/dever.json`
- 核心 model：维护本组件后补充具体路径。
- 核心 Service/API：维护本组件后补充具体路径。

## 规则

- 写 Service/API 前，先复用 package/front page JSON 能力。
- 不创建 CRUD wrapper Service/API。
- 不修改生成文件或编译后的 front 产物。
- 组件专属行为留在本组件内，不散落到全局。
- 不为禁止字段保留适配代码，不保留双路径备用实现。
- 任何公开 API、权限、密钥、外部调用或升级影响，都要在这里写清楚。
- 新增菜单、权限、公开路径、站点默认展示配置时，优先写入组件 `dever.json.front.sites.<site>.config`；项目覆盖只写 `config/front.json`。
- 新增服务端模板页面时，保留 page JSON 六对象规则，使用 `page.render: "template"` + 顶层 `template`，SEO 放 `data.seo`。
- 删除或改名页面、model、Service/API 前，先写清影响范围和替代路径。

## 允许写 Service/API 的场景

- 跨表事务。
- 状态流转。
- 外部系统调用。
- 登录、注册、回调、webhook。
- front 插件需要的自定义交互接口。
- package/front 标准 page action 无法表达的真实业务动作。

普通列表、新增、编辑、删除、详情不要写 Service/API。

## Front 插件规则

- 只有 page JSON 表达不了交互时才写插件节点。
- `plugin.ts` 只注册节点，不做请求、配置读取或全局副作用。
- 节点名加组件前缀，避免和内置节点冲突。
- 插件只依赖公开 SDK，不直接 import 主 `front/src`。

## 常见检查

- 修改 page JSON 前先读现有 model。
- 新增 Provider/API 前先读现有 service。
- 修改后台页面前先检查组件权限。
- 修改 `dever.json` 后检查 `skills`、`public`、`sites`、`depends` 是否仍有效。

## 升级影响

组件发布新版本时，需要记录：

- 需要新增或更新的 package/module。
- 需要写入的 `dever.json` 元数据。
- 是否需要更新项目 `config/front.json(c)` 菜单、权限或 public 配置。
- 是否有破坏性数据变更。
